# Kingdom Survival — Development Status

Последнее обновление: 2026-09-02

> Технический журнал фактически реализованного состояния Unity-проекта. Актуальный канон основной игры: `KINGDOM_SURVIVAL_GAME_CONCEPT_CANON_RU_v1_14.md`.
>
> Подробный журнал предыдущих этапов до базы полей боя сохранён в `ProjectDocs/Archive/DEVELOPMENT_STATUS_2026-09-02_PRE_BATTLEFIELD_DATABASE.md`.

## 1. Текущий этап

Боевой полигон `Assets/_Project/Scenes/BattleSandbox.unity` использует единую `Базу существ` для типов бойцов и существ и новую отдельную `Базу полей боя` для фоновых 2D-иллюстраций тактической сцены.

Основная сцена `Prototype_Main.unity`, `GameState`, стратегический `BattleSystem`, карта, гарнизоны, сохранения и актуальный канон v1.14 этим этапом не изменены.

## 2. База существ и sandbox — текущее рабочее состояние

- Unity-окно `Kingdom Survival → База существ` хранит типы, характеристики, теги, портреты и отдельные миниатюры поля;
- девять стабильных `typeId`: шесть бойцов и три типа зверей;
- портреты используют вертикальное кадрирование 3:4 через `ScaleAndCrop`, отдельные масштаб и смещение;
- полевая миниатюра имеет собственные `battlefieldScale / battlefieldOffset` и не связана с кадрированием портрета;
- sandbox использует компактную pointy-top арену `9 × 9` из 81 гекса: нечётные горизонтальные ряды смещены вправо на половину гекса, чётные остаются без смещения;
- движение и 1 ОД разделены, остаток движения можно тратить несколькими перемещениями;
- обычный гекс стоит 1 движения, сложный — 2, непроходимый блокирует путь;
- ближняя атака поддерживает два способа наведения: центр цели автоматически выбирает лучший доступный подход, а край гекса сохраняет ручной выбор конкретной грани; в обоих случаях противник должен входить в границы оставшегося движения;
- дальняя атака не зависит от стороны гекса и при необходимости автоматически выбирает достижимую клетку стрельбы;
- атака и перемещение имеют текущие анимации, HP и детерминированный sandbox-урон;
- реальные миниатюры не получают старую цветную круглую заглушку;
- HP-полосы являются отдельными UI Toolkit-элементами и следуют за миниатюрами;
- боевой курсор меча/выстрела рисуется отдельным верхним `attackCursorOverlay` поверх миниатюр и HP;
- белое кольцо текущего участника имеет alpha 0.5, голубое кольцо защитной стойки не менялось;
- зона доступного движения отображается только голубым контуром гексов без сплошной голубой заливки; alpha контура снижена до `0.40`, чтобы подсветка была примерно наполовину прозрачной;
- базовые гексы и сетка стали примерно на 20% прозрачнее, чтобы фон поля боя читался лучше.

Экспериментальные характеристики и формула sandbox не считаются каноном основной игры.

## 3. Этап: база полей боя

Реализовано 2026-09-02.

### Что работает

- добавлен отдельный модуль `Assets/_Project/BattlefieldDatabase/`;
- добавлено Unity-окно `Kingdom Survival → База полей боя`;
- каждое поле хранит стабильный `ID`, отображаемое название, фоновый `Sprite`, масштаб, смещение и теги;
- доступно добавление, дублирование, удаление и проверка записей;
- отдельный справочник тегов хранит ID, название, категорию, цвет и описание;
- поле можно назначить активным для `BattleSandbox` кнопкой `ИСПОЛЬЗОВАТЬ В BATTLESANDBOX`;
- предпросмотр показывает фоновую иллюстрацию с актуальной тестовой сеткой `9 × 9` в той же чередующейся раскладке рядов, что и runtime-поле;
- кнопка `СБРОСИТЬ КАДРИРОВАНИЕ` возвращает масштаб к 1 и смещение к 0 / 0;
- смещение хранится относительно размеров viewport, поэтому не привязано к конкретному разрешению;
- стартовая запись `forest_clearing_01 / Лесная поляна` уже использует Sprite из `Assets/_Project/Art/Battlefields/`;
- стартовые теги: лес, поляна, болото, руины и мрачная атмосфера;
- runtime загружает активное поле из `Resources/BattlefieldDatabase/KingdomSurvivalBattlefields`;
- фон подключается только в сцене `BattleSandbox` и не затрагивает основную игру;
- при успешном доступе к цветам sandbox гексы получают прозрачную палитру: обычные клетки почти прозрачны, сложные/непроходимые сохраняют отдельные оттенки, а зона движения рисуется только отдельным голубым контуром;
- если окружение не разрешает менять `static readonly` цвета через reflection, используется безопасный fallback-слой: иллюстрация всё равно отображается, а существующий код боя продолжает работать;
- миниатюры бойцов, HP и верхний боевой курсор остаются самостоятельными UI-слоями;
- сетка, маршруты, подсветки и юниты не записываются в изображение поля и продолжают формироваться игрой.

### Файлы

Добавлен модуль:

- `Assets/_Project/BattlefieldDatabase/Runtime/BattlefieldDatabaseAsset.cs`;
- `Assets/_Project/BattlefieldDatabase/Runtime/BattlefieldDatabaseBootstrap.cs`;
- `Assets/_Project/BattlefieldDatabase/Runtime/KingdomSurvival.BattlefieldDatabase.asmdef`;
- `Assets/_Project/BattlefieldDatabase/Editor/BattlefieldDatabaseWindow.cs`;
- `Assets/_Project/BattlefieldDatabase/Editor/KingdomSurvival.BattlefieldDatabase.Editor.asmdef`;
- `Assets/_Project/BattlefieldDatabase/Resources/BattlefieldDatabase/KingdomSurvivalBattlefields.asset`;
- `Assets/_Project/BattlefieldDatabase/Tests/EditMode/BattlefieldDatabaseTests.cs`;
- `Assets/_Project/BattlefieldDatabase/Tests/EditMode/KingdomSurvival.BattlefieldDatabase.EditModeTests.asmdef`;
- соответствующие `.meta`.

Также обновлён текущий `ProjectDocs/DEVELOPMENT_STATUS.md`; предыдущая подробная версия журнала перенесена в `ProjectDocs/Archive/` без изменения её содержимого.

## 4. Изоляция и канон

- `KingdomSurvival.BattlefieldDatabase` зависит только от Unity runtime API и не добавляет зависимость чистой модели `KingdomSurvival.BattleSandbox.Runtime` от `UnityEngine`;
- editor-код базы полей находится в отдельной editor-only assembly;
- база полей не меняет боевые правила, AI, формулу урона, составы, сцену основной кампании или сохранения;
- канон `KINGDOM_SURVIVAL_GAME_CONCEPT_CANON_RU_v1_14.md` не изменён: база полей является инструментом и визуальным слоем в рамках уже утверждённого 2D-направления.

## 5. Проверки

Проверено статически в доступном окружении:

- актуальный базовый `main` перед переносом этапа: `d57484cb5dd6ad08e598764cf9222e0ffe015d5a`;
- последний существующий commit `Keep attack cursors above battlefield units` сохранён как родитель и не перезаписывается;
- Sprite лесной поляны уже импортирован Unity как `Sprite` и его GUID/fileID совпадают с записью базы;
- GUID `BattlefieldDatabaseAsset.cs.meta` совпадает со ссылкой `m_Script` в `KingdomSurvivalBattlefields.asset`;
- новые runtime/editor/test `.asmdef` разделены по назначению;
- база содержит уникальный `forest_clearing_01`, валидные теги и назначенный фон;
- добавлено 3 EditMode-теста базы полей: загрузка активного поля, уникальность ID и отсутствие ошибок валидации;
- после текущего этапа ожидается 75 EditMode-тестов: прежние 74 плюс новый тест топологии чередующихся рядов;
- runtime имеет fallback на случай запрета reflection для `static readonly` палитры и не должен оставлять сцену без изображения;
- канон и стратегическая кампания не менялись.

Ограничение проверки:

- в подключённом окружении нет Unity Editor и C#-компилятора, поэтому фактическую Unity-компиляцию, EditMode Test Runner и визуальный порядок слоёв здесь запустить нельзя.

## 6. Что проверить после pull

1. Дождаться завершения Unity-компиляции без ошибок.
2. Открыть `Kingdom Survival → База полей боя`.
3. Убедиться, что слева есть `Лесная поляна` с ID `forest_clearing_01` и её миниатюра.
4. Выбрать поле и проверить большой preview: изображение должно быть под сеткой `9 × 9`, где каждый второй ряд смещён на половину гекса.
5. Изменить `Масштаб` и `Смещение X / Y`; preview должен обновляться.
6. Нажать `СБРОСИТЬ КАДРИРОВАНИЕ`; значения должны вернуться к `1 / 0 / 0`.
7. Добавить временную запись, назначить другой Sprite и убедиться, что его можно сделать активным для sandbox.
8. Нажать `ПРОВЕРИТЬ БАЗУ`; на стартовых данных ошибок быть не должно.
9. Открыть `Assets/_Project/Scenes/BattleSandbox.unity`, нажать Play и начать бой.
10. Проверить, что лесная поляна видна под гексами, а обычные гексы не превращают сцену обратно в сплошное тёмное поле.
11. Проверить сложные и непроходимые клетки, а также доступные клетки: зона движения должна читаться полупрозрачными голубыми контурами без сплошной заливки.
12. Проверить маршрут, меч/выстрел, миниатюры, HP-полосы и всплывающий урон — они должны оставаться поверх фона.
13. Проверить, что верхний значок атаки из commit `d57484c` по-прежнему находится поверх миниатюр.
14. Запустить EditMode-тесты; ожидается 75 успешных тестов.
15. Открыть `Prototype_Main.unity` и убедиться, что основная игра запускается без изменений.

## 7. Следующий шаг

После ручной проверки базы полей можно отдельно решить, должна ли запись поля хранить собственную раскладку сложных/непроходимых гексов, стартовые позиции и препятствия. Сейчас это намеренно не добавлено, чтобы база оставалась минимальным инструментом для загрузки и кадрирования 2D-фона.

## 8. Hotfix: портрет в подробной карточке боя

Реализовано 2026-09-02 после обнаружения, что назначенный портрет виден в шкале инициативы, но мог не отображаться в подробной карточке, открытой с поля боя.

### Что исправлено

- подтверждено, что шкала инициативы и ПКМ по гексовому полю используют один и тот же `typeId` и один источник `Portrait` из базы существ;
- подробная карточка теперь сама восстанавливает портрет через `UnitDatabaseAsset` по `typeId`, если вызывающий код не передал Sprite;
- это автоматически закрывает прежний путь ЛКМ по союзнику на поле, где карточка открывалась вообще без аргумента `portrait`;
- окно подробной карточки переводится в видимое состояние до применения Sprite и кадрирования, чтобы `Image` уже имел рабочую геометрию UI Toolkit;
- на следующем UI-tick выполняется повторная синхронизация портрета и `MarkDirtyRepaint`, что устраняет зависимость от первого layout-pass скрытого окна;
- сохраняются существующие `ScaleAndCrop`, `portraitScale` и `portraitOffset` из базы существ;
- при закрытии карточки ссылка Sprite на её `Image` очищается, чтобы следующий открытый тип не показывал старый портрет на промежуточном кадре;
- карточки выбора и инициативы также используют тот же безопасный resolver портрета, при этом их внешний вид и логика не меняются;
- fallback-текст остаётся только для типа, у которого в базе действительно нет портрета.

### Изменённые файлы

- `Assets/_Project/BattleSandbox/UI/SandboxFighterPresentation.cs`;
- `ProjectDocs/DEVELOPMENT_STATUS.md`.

Боевые правила, поле боя, фон, HP, инициатива, формула урона, AI, база данных существ и канон v1.14 не изменены.

### Проверено статически

- `ResolvePortrait` сначала использует явно переданный Sprite, затем ищет `UnitDefinitionData.Portrait` по стабильному `typeId`;
- `Open` показывает detail-window до первого `RefreshValues`;
- повторный refresh планируется через `window.schedule.Execute(...).ExecuteLater(1)` и защищён проверкой открытого `typeId`;
- `RefreshValues` повторно разрешает портрет по базе, применяет существующее кадрирование и вызывает `MarkDirtyRepaint`;
- отдельное изменение `BattleSandboxController.cs` не требуется: текущий ПКМ уже передаёт правильный портрет, а старый ЛКМ-путь теперь обслуживается resolver-ом самой карточки.

Ограничение проверки:

- в подключённом окружении нет Unity Editor и C#-компилятора, поэтому фактическую компиляцию и визуальную проверку нужно выполнить после pull.

После pull проверить:

1. открыть бой и нажать ПКМ по союзнику с назначенным портретом — большой портрет должен появиться сразу;
2. нажать ПКМ по противнику с назначенным портретом и проверить тот же результат;
3. открыть подробности из шкалы инициативы и сравнить кадрирование с карточкой, вызванной с поля;
4. нажать ЛКМ по союзнику на поле: подробная карточка теперь также должна получить его портрет;
5. временно убрать портрет у одного типа и убедиться, что только у него появляется текстовая заглушка;
6. проверить повторное быстрое открытие карточек разных типов: предыдущий Sprite не должен мелькать в новой карточке.

## 9. Hotfix: контур зоны движения и прозрачность гексов

Реализовано 2026-09-02.

### Что изменено

- доступные для перемещения клетки больше не заменяют свою заливку сплошным голубым цветом;
- каждая доступная клетка сохраняет собственную заливку местности и получает отдельный голубой контур толщиной `2.2 px`;
- текущая клетка активного бойца исключена из дополнительного голубого контура, чтобы не спорить с белым кольцом текущего бойца;
- базовый контур всей гексовой сетки получил alpha `0.8`, то есть стал примерно на 20% прозрачнее;
- базовые fallback-заливки обычной, сложной и непроходимой клетки получили alpha `0.8`;
- при активном фоне поля runtime-палитра дополнительно уменьшена примерно на 20%: normal `0.05 → 0.04`, difficult `0.24 → 0.192`, impassable `0.48 → 0.384`;
- голубой `ReachableColor` теперь используется именно как цвет контура зоны движения, а не как заливка;
- красная заливка выбранной цели также снижена до alpha `0.8`;
- маршрут при наведении, жёлтая позиция атаки, курсор меча/выстрела, HP, миниатюры и боевые правила не изменены.

### Изменённые файлы

- `Assets/_Project/BattleSandbox/UI/HexBoardElement.cs`;
- `Assets/_Project/BattlefieldDatabase/Runtime/BattlefieldDatabaseBootstrap.cs`;
- `ProjectDocs/DEVELOPMENT_STATUS.md`.

### Проверено статически

- расчёт `battle.GetReachable(current.Id)` не изменён;
- `DrawBoard` сначала рисует обычную сетку, затем отдельным проходом только контуры доступных клеток;
- `DrawHex` получил необязательный параметр `fillShape`, существующие вызовы сохраняют прежнее поведение по умолчанию;
- контур зоны движения не рисуется во время анимации и не добавляется на текущую клетку бойца;
- runtime-палитра базы полей синхронизирована с новой семантикой `ReachableColor`;
- канон v1.14 не менялся.

Ограничение проверки:

- Unity Editor и C#-компилятор в подключённом окружении недоступны, поэтому финальную визуальную проверку нужно сделать после pull.

После pull проверить в `BattleSandbox`:

1. зона движения должна состоять из голубых контуров гексов без голубой сплошной заливки;
2. фон лесной поляны должен быть заметно лучше виден сквозь обычные, сложные и непроходимые клетки;
3. обычная сетка должна выглядеть примерно на 20% мягче;
4. голубой контур зоны движения должен оставаться достаточно читаемым на светлых и тёмных участках фона;
5. маршрут, жёлтый гекс позиции атаки, меч/выстрел, миниатюры и HP должны отображаться как раньше.

## 10. Hotfix: центральная зона автоподбора ближней атаки

Реализовано 2026-09-02.

### Что изменено

- у ближней атаки добавлена центральная зона наведения радиусом `48%` от размера гекса цели;
- при наведении в эту центральную область игра использует существующий `TryFindAttackPosition` и автоматически выбирает лучшую доступную сторону для подхода к цели;
- критерий автоподбора не меняет боевые правила: выбирается только сторона, которая уже разрешена текущей дальностью движения, препятствиями и занятостью клеток;
- меч по-прежнему отображается на конкретной грани, откуда фактически будет выполнена атака, поэтому выбранную системой сторону видно до клика;
- при наведении ближе к краю гекса сохраняется прежний ручной выбор конкретной стороны через направление курсора;
- если вручную выбранная сторона недоступна, атака с неё остаётся запрещённой; система не подменяет ручной выбор другой гранью;
- если курсор находится в центре, но ни с одной стороны подойти нельзя, показывается отдельное сообщение о невозможности подойти в пределах оставшегося движения;
- дальняя атака, формула урона, расход движения, 1 ОД, анимации и AI не изменены.

### Изменённые файлы

- `Assets/_Project/BattleSandbox/UI/HexBoardElement.cs`;
- `ProjectDocs/DEVELOPMENT_STATUS.md`.

### Проверено статически

- `MeleeAutoSelectRadiusScale = 0.48f` влияет только на разделение центральной и краевой зоны указателя;
- центр использует уже существующий `battle.TryFindAttackPosition`, который перебирает допустимые соседние гексы и выбирает вариант с минимальной стоимостью движения;
- край гекса продолжает использовать `SelectMeleeAttackPosition` и `TryGetMeleeAttackPosition` без изменения их правил;
- `hoverAttackPosition` получает фактически выбранную клетку и остаётся источником для маршрута и выполнения атаки;
- повторный `UpdatePointerPreview` в момент клика использует тот же алгоритм, поэтому предпросмотр и фактическая команда не расходятся;
- канон v1.14 не изменён.

Ограничение проверки:

- Unity Editor и C#-компилятор в подключённом окружении недоступны, поэтому визуальный порог между центром и ручным выбором стороны нужно проверить после pull.

После pull проверить в `BattleSandbox`:

1. навести курсор прямо в центр врага — значок меча должен появиться, если хотя бы с одной стороны атака доступна;
2. убедиться, что меч показывается на грани, которую система автоматически выбрала для подхода;
3. кликнуть в центре и проверить, что боец идёт к этой грани и атакует;
4. затем вести курсор к разным краям того же гекса — меч должен переключаться между соответствующими сторонами;
5. проверить занятую или недоступную грань: у края она должна оставаться запрещённой, а в центре система должна подобрать другую допустимую сторону, если она существует;
6. проверить врага полностью вне границ оставшегося движения — атака не должна становиться доступной ни из центра, ни с края;
7. проверить лучника: поведение дальнего выстрела должно остаться прежним.

## 11. Этап: поле 9 × 9 в раскладке King’s Bounty

Реализовано 2026-09-02 по присланному пользователем референсу боевой арены.

### Что изменено

- стандартная sandbox-арена теперь состоит из `9 × 9 = 81` pointy-top гекса;
- используется компактная odd-row раскладка: чётные ряды без смещения, нечётные смещены вправо на половину ширины гекса; накопительного ухода каждого следующего ряда вправо больше нет;
- `HexCoord` переведён с прямого axial-представления на прямоугольные odd-row координаты `Q = колонка / R = ряд`;
- набор шести соседей теперь зависит от чётности ряда, а `DistanceTo` переводит offset-координаты во внутренние cube-координаты и сохраняет корректную гекс-дистанцию;
- `SandboxBattle`, `SandboxMovementPath`, ближняя/дальняя атака и AI продолжают использовать общие `Neighbors()` и `DistanceTo()`, поэтому логика движения и дальности совпадает с новой визуальной сеткой;
- стартовые позиции бойцов перенесены к левой стороне арены, противников — к правой; центральная часть остаётся свободной для манёвра;
- тестовые сложные и непроходимые клетки переразложены под высоту 9 рядов;
- `HexBoardElement` рассчитывает ширину поля как `9 + 0.5` шага по горизонтали и смещает только нечётные ряды;
- предпросмотр в `Kingdom Survival → База полей боя` синхронизирован с runtime: `9 × 9` и та же odd-row формула расположения;
- голубой контур доступного движения уменьшен по alpha с `0.78` до `0.40` и в fallback-палитре, и в runtime-палитре базы полей;
- центральный автоподбор стороны ближней атаки и ручной выбор конкретной стороны у края гекса сохранены без изменения правил.

### Изменённые файлы

- `Assets/_Project/BattleSandbox/Runtime/HexCoord.cs`;
- `Assets/_Project/BattleSandbox/Runtime/SandboxRoster.cs`;
- `Assets/_Project/BattleSandbox/UI/HexBoardElement.cs`;
- `Assets/_Project/BattleSandbox/Tests/EditMode/BattleSandboxTests.cs`;
- `Assets/_Project/BattlefieldDatabase/Runtime/BattlefieldDatabaseBootstrap.cs`;
- `Assets/_Project/BattlefieldDatabase/Editor/BattlefieldDatabaseWindow.cs`;
- `ProjectDocs/DEVELOPMENT_STATUS.md`.

### Проверено статически

- перед реализацией удалённый `main` был `e5c242aabbdcb9dc47818d53b9cb95229872a371`; его несвязанные изменения Build Profile, URP settings, двух database asset-файлов и solution-файла сохранены как основа и не перезаписываются;
- для чётного и нечётного ряда добавлен EditMode-тест, подтверждающий разные диагональные соседства;
- проверка расстояния использует новую odd-row → cube конвертацию и остаётся симметричной;
- тест обхода заблокированной клетки по-прежнему требует три шага, то есть восстановление маршрута работает через новую топологию соседей;
- стандартная битва создаётся как `9 × 9`, все стартовые позиции находятся внутри поля;
- UI-hit-test перебирает те же `9 × 9` координаты, которые рисует `HexLayout`;
- runtime-поле и editor-preview используют одинаковое чередование смещения `0 / 0.5`;
- ожидаемое текущее число EditMode-тестов — 75;
- канон v1.14, формула урона, 1 ОД, стоимость местности и правила атаки не изменены.

Ограничение проверки:

- Unity Editor и C#-компилятор в подключённом окружении недоступны, поэтому Unity-компиляцию, Test Runner и визуальную проверку новой сетки нужно выполнить после pull.

После pull проверить в `BattleSandbox`:

1. поле должно содержать ровно 81 гекс: 9 рядов по 9 клеток;
2. каждый второй ряд должен быть смещён на половину гекса и возвращаться обратно на следующем ряду, без диагонального «уползания» всей сетки;
3. ваши бойцы должны начинать слева, звери справа, центральная часть поля — оставаться доступной для манёвра;
4. голубой контур доступного движения должен быть заметно мягче и иметь примерно половинную прозрачность;
5. провести несколько маршрутов по горизонтали и диагонали: линия маршрута и фактическое движение должны проходить только между визуально соседними клетками;
6. проверить атаку из центра вражеской клетки и ручной выбор разных граней у края — обе схемы должны соответствовать новой геометрии;
7. проверить лучника и дальность: визуальное количество гексов и расчёт дальности должны совпадать;
8. открыть `Kingdom Survival → База полей боя`: preview должен повторять runtime-сетку 9 × 9;
9. запустить EditMode-тесты; ожидается 75 успешных тестов;
10. убедиться, что настройки поля и базы существ из commit `e5c242a` остались без изменений.
