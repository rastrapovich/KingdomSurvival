# Kingdom Survival — Development Status

Последнее обновление: 2026-09-01

> Технический журнал фактически реализованного состояния Unity-проекта. Актуальный канон после этого этапа: `KINGDOM_SURVIVAL_GAME_CONCEPT_CANON_RU_v1_9.md`.

## 1. Текущий этап

**Этап стратегического автобоя реализован; текущая итерация — упрощение боевого UI и перенос подробностей в личное окно бойца.**

Основной прототип по-прежнему проверяет конфликт «экспедиция или оборона столицы»:

- один город;
- одна армия;
- одна активная экспедиция;
- войска экспедиции не защищают столицу;
- стратегическое время непрерывное, с паузой и скоростями;
- значимые окна используют автопаузу и очередь;
- теперь экспедиционные и оборонительные столкновения используют один детерминированный боевой расчёт.

## 2. Что реализовано в этой итерации

### Детерминированный `BattleSystem`

Добавлен отдельный доменный слой `BattleSystem`:

- `BattleContext` хранит источник боя, участников, противника, местность, теги и выбранную доктрину;
- `BattleResult` хранит один конкретный рассчитанный исход;
- прогноз и применение используют один и тот же сохранённый `BattleResult`;
- подтверждение боя не выполняет повторный расчёт и не перебрасывает потери;
- случайность внутри боевого расчёта отсутствует.

### Три фазы боя

Каждый бой рассчитывается в трёх фазах:

1. `Сближение`;
2. `Столкновение`;
3. `Удержание/отход`.

В отчёте сохраняются счёт игрока/противника и сработавшие модификаторы каждой фазы.

### Боевые роли и характеристики

Боевой профиль привязан к стабильному ID бойца, а не к отображаемой русской строке роли.

Для текущих пяти бойцов определены внутренние коды:

- гвардеец;
- лучник;
- лекарь;
- копейщик;
- разведчик.

Боевой профиль хранит:

- атаку;
- защиту;
- максимальные HP;
- текущие HP;
- состояние здоровья.

Разведчик влияет на засаду/сближение, лучник — на ранний урон, гвардеец — на удержание линии, копейщик — на натиск/зверей, лекарь снижает тяжесть одного последствия.

### Доктрины

Перед подтверждением значимого боя можно переключать:

- `ОСТОРОЖНАЯ`;
- `СБАЛАНСИРОВАННАЯ`;
- `НАТИСК`.

Каждое переключение обновляет точный прогноз, но ничего не применяет. Кнопка `ПОДТВЕРДИТЬ БОЙ` применяет только текущий сохранённый результат.

### HP, ранения и смерть

Состояния бойца:

`здоров → ранен → тяжело ранен → погиб`.

Правила прототипа:

- полностью здоровый боец не погибает за один бой даже при разгроме или `Натиске`;
- смерть возможна для уже повреждённого/тяжелораненого бойца при критическом результате и заранее отображается в прогнозе;
- лекарь может понизить тяжесть одной рассчитанной травмы;
- погибший удаляется из `GameState.Fighters` и из списка бойцов активной экспедиции;
- состояние выживших сохраняется после боя.

### Восстановление по игровому времени

`ContinuousSimulationSystem.Advance()` передаёт реально прошедшие игровые часы в `BattleSystem.AdvanceCapitalRecovery()`.

- лечатся только бойцы, фактически находящиеся в столице;
- бойцы активной экспедиции не восстанавливают HP;
- на паузе лечение не идёт;
- ускорение времени ускоряет восстановление вместе с остальной стратегической симуляцией.

### Тестовый бой в Чёрном лесу

Для локации `forest` добавлено обязательное столкновение `Засада в Чёрном лесу`:

- противник: лесные твари;
- учитываются теги `засада` и `звери`;
- при известном прибытии обычное решение исследования уступает место обязательному боевому прогнозу;
- если Чёрный лес найден по пути, бой подготавливается после решения остаться/исследовать находку;
- победа или тяжёлая победа помечает тестовое столкновение завершённым;
- отход/разгром запускает реальное возвращение с текущей позиции.

### Нападение на столицу

Старый фиксированный кризис амбара заменён боевым столкновением:

- момент появления кризиса остаётся случайным событием суток;
- сам бой после появления полностью детерминирован;
- в оборону входят только бойцы, оставшиеся в столице;
- командир даёт бонус только если находится в замке;
- исход определяет потери бойцов и возможные потери пищи/настроения;
- DEBUG-кнопка кризиса по-прежнему позволяет принудительно проверить нападение.

### Карточки и личное окно бойца

Добавлен `PrototypeUIController.FighterDetails.cs`:

- обычные карточки бойцов в двух гарнизонах стали компактнее;
- на каждой карточке постоянно видна шкала текущих HP;
- подробные `уровень / атака / защита / HP / состояние / местонахождение` больше не дублируются общим текстовым списком на экране армии;
- ПКМ по карточке открывает отдельное личное окно бойца;
- личное окно не ставит стратегическое время на паузу и обновляет HP во время лечения;
- ПКМ остаётся доступным и во время экспедиции, когда перенос бойцов заблокирован;
- ЛКМ и drag-and-drop между гарнизонами сохраняют прежнее назначение.

### Упрощённый UI автобоя

Переработан `PrototypeUIController.Battle.cs` без изменения `BattleSystem`:

- слева отображаются карточки фактических бойцов отряда;
- справа отображается одна агрегированная карточка противника, соответствующая текущей модели `EnemyPower`;
- центр оставлен под `VS`, короткий прогноз исхода и только значимые стратегические последствия;
- внутренние очки трёх фаз и длинные объяснения убраны из предбоевого окна, но сохраняются в полном отчёте `Королевских донесений`;
- точный прогноз потерь сохранён: карточка каждого бойца показывает ожидаемое изменение HP и прогнозную шкалу здоровья до подтверждения;
- три доктрины собраны в одну компактную горизонтальную строку;
- ПКМ по карточке бойца прямо в окне боя также открывает его личное окно;
- подтверждение по-прежнему применяет тот же заранее сохранённый `BattleResult`, без повторного расчёта;
- пока существует неподтверждённый бой, блокирующая модальная система не возобновляет время.

## 3. Сохранённые предыдущие системы

Без изменений должны продолжать работать:

- непрерывное время `День N · HH:MM`;
- скорости `×1 / ×3 / ×5 / ×10`;
- A* и смена маршрута от текущей позиции;
- постепенное исчезновение пройденных точек;
- исследование руин за 2 часа и шахты за 5 часов;
- дорожные действия и трёхчасовой сбор ягод;
- ускорение переноса снабжения при удержании;
- автопауза обязательных событий и автоматическое продолжение после последнего окна;
- сохранение ручной паузы;
- гарнизоны командира/столицы и drag-and-drop бойцов.

## 4. Файлы этой итерации

Новые:

- `Assets/_Project/UI/PrototypeUIController.FighterDetails.cs`;
- `Assets/_Project/UI/PrototypeUIController.FighterDetails.cs.meta`.

Изменены:

- `Assets/_Project/UI/PrototypeUIController.Battle.cs`;
- `ProjectDocs/DEVELOPMENT_STATUS.md`.

Канон `KINGDOM_SURVIVAL_GAME_CONCEPT_CANON_RU_v1_9.md` не меняется: эта итерация перерабатывает представление уже утверждённой боевой системы, а не её правила.

## 5. Автоматические проверки

Боевые правила этой итерацией не менялись, поэтому набор `BattleSystemTests` остаётся прежним: ожидается `39` EditMode-тестов всего.

Для новых/изменённых UI-файлов выполнены статические проверки структуры C#, парности скобок, ссылок на существующие поля `BattleSystem`, отсутствия повторного расчёта результата и сохранения левого drag-and-drop пути. В текущем окружении по-прежнему нет Unity Editor, `dotnet`, `csc` или `mcs`, поэтому обязательная финальная проверка — импорт и Test Runner после pull.

## 6. Что проверить после pull

1. Открыть проект и дождаться завершения компиляции. Console должна быть без compile errors.
2. `Window → General → Test Runner → EditMode` → запустить `KingdomSurvival.Core.EditModeTests`; ожидается `39` тестов.
3. Открыть экран `Армия`: старого общего блока `БОЕВОЕ СОСТОЯНИЕ` быть не должно; на каждой карточке должна быть видна компактная HP-шкала.
4. Проверить ЛКМ и drag-and-drop между гарнизонами: поведение должно остаться прежним.
5. Нажать ПКМ по бойцу в столице: должно открыться личное окно с ролью, уровнем, атакой, защитой, большой HP-шкалой, числом HP, состоянием и местонахождением.
6. Оставить личное окно раненого бойца открытым и запустить время в столице: HP и шкала должны обновляться без постановки игры на паузу.
7. Отправить отряд в экспедицию: ПКМ по его карточкам должен по-прежнему открывать сведения, но ЛКМ/перенос не должны менять состав до возвращения.
8. Через DEBUG вызвать кризис столицы: в окне автобоя слева должны быть только реальные защитники столицы, справа — агрегированная карточка противника.
9. Переключать `ОСТОРОЖНАЯ / СБАЛАНСИРОВАННАЯ / НАТИСК`: короткий прогноз и ожидаемые HP на карточках должны меняться, реальные HP до подтверждения — нет.
10. Нажать ПКМ по карточке бойца внутри окна автобоя: должно открыться то же личное окно бойца.
11. Подтвердить бой: фактические HP должны совпасть с показанным прогнозом, а подробные три фазы должны остаться в `Королевских донесениях`.
12. Добраться до Чёрного леса и повторить проверку окна боя для экспедиционного состава.
13. Коротко перепроверить регрессии: маршрут, ягоды, исследования, автопауза, удержание снабжения и возврат армии.

## 7. Следующий рекомендуемый этап

После зелёной компиляции, `39/39` EditMode и проверки нового интерфейса — сначала оценить читаемость карточек и личного окна на реальном разрешении. Следующим отдельным решением можно определить, нужен ли противнику собственный состав/HP отдельных существ или агрегированная карточка `EnemyPower` остаётся достаточной для стратегического автобоя.
