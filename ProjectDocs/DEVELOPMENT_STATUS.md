# Kingdom Survival — Development Status

Последнее обновление: 2026-09-10

> Технический журнал фактически реализованного состояния Unity-проекта и зафиксированных проектных решений.
>
> Актуальный общий канон: `KINGDOM_SURVIVAL_GAME_CONCEPT_CANON_RU_v1_28.md`.
>
> Единая энциклопедия мира: `ProjectDocs/LORE.md`.
>
> Единая нарративная библия: `ProjectDocs/NARRATIVE.md`.
>
> Движок: Unity 6000.5.10f1. UI: UI Toolkit.

## 0. Архив технического журнала

Полный журнал состояния до внедрения индивидуальной кадрировки портретов сохранён без изменений в `ProjectDocs/Archive/DEVELOPMENT_STATUS_2026-09-10_before_portrait_framing.md`. Более ранняя история по-прежнему находится в `ProjectDocs/Archive/DEVELOPMENT_STATUS_2026-09-08_before_condition_showcase.md`.

Текущий файл снова сокращён до реально актуального состояния, чтобы им можно было пользоваться как рабочей точкой входа. Ничего из предыдущего журнала не потеряно: подробные записи P00–P04, Narrative Check Presentation, группировки истории диалога, Dialogue Graph/Inspector и связанных исправлений находятся в архивной копии.

## 1. Текущий этап

Проект находится на этапе **«Нарративный фундамент / production-проход первой главы»**. Сохраняется технический фундамент карты, непрерывного времени, экспедиций, строительства, Hero Screen, Unit Database, BattleSandbox, Dialogue Database, нарративных проверок и личного экрана «Этапы разработки».

Главный производственный приоритет остаётся прежним:

`герой → нормальная жизнь Дома → паводок/ремонт → причинные симптомы → расследование → нижние люди и старое соглашение → возвращение → последствия → главная тайна → антагонистическая сила → первый регион → первый большой квест`.

Новые крупные универсальные системы до доказательства первого региона не являются приоритетом.

## 2. Подтверждённый технический baseline

Последний реально подтверждённый пользователем baseline до текущей правки: Unity компилировался чисто, полный EditMode-набор проходил **213/213**, вручную были проверены карта, непрерывное время, экспедиция, Hero Screen, База существ и База диалогов. Последующие Editor/UI-изменения, описанные ниже, требуют нового прогона после Pull.

В первой главе уже существуют: каркас N01–N17, четыре постоянных жителя Дома (Остафий, Лада, Мирон, Ульяна), production-проход N01–N03, базовая рифма состояния Дома N01→N16, система пассивных/активных проверок и presentation-слой диалоговой истории. Предыдущие подробности сохранены в архиве из §0.

## 3. Актуальный Dialogue Database / Graph / Inspector

Актуальный `main` перед этой правкой — `112ff8937855c9537ff668267c794176c2a0239a` (`Полноценное редактирование текста в нодах графа + переработка Inspector'а`). Эта версия сохранена целиком при переносе текущей работы: прямых файловых пересечений с кадрированием портретов не было.

На текущем фундаменте:

- литературный текст выбранной ноды можно редактировать прямо на canvas;
- Standard/Full показывают полный текст без искусственного обрезания, Compact остаётся обзорным;
- высота карточек считается по реальному содержимому;
- Inspector прокручивается как единая панель, открывается сверху при смене узла и не должен отдавать колесо canvas-зуму;
- Inspector показывает портретный заголовок говорящего;
- legacy-текст скрывается из основного UI, когда узел уже использует `textBlocks`.

Эта часть пришла из актуального `main` и текущей задачей не переписывалась.

## 4. Индивидуальная кадрировка портретов говорящих — 10.09.2026

### 4.1. Что реализовано

Система портретов диалога переведена на двухуровневую модель:

1. **Общая композиция экрана** задаётся в `Kingdom Survival → UI Конструктор → Диалог → Портрет`:
   - размер и положение рамки;
   - режим изображения `Cover / Contain / Stretch`;
   - общий `Scale`;
   - общий `Offset`;
   - `Tint`;
   - `Opacity`.
2. **Индивидуальная кадрировка конкретного говорящего** хранится в `DialogueSpeakerData` и накладывается поверх общей композиции:
   - `overridePortraitFraming`;
   - `portraitScale`;
   - `portraitOffsetNormalized`;
   - `portraitFlipX`.

Если индивидуальная настройка выключена, портрет использует только общую композицию UI Конструктора. Это позволяет один раз определить дизайн диалогового экрана, а затем подправлять только проблемные портреты разных пропорций, не двигая рамку для всех персонажей.

### 4.2. Runtime

`PrototypeUIController.Narrative.cs` больше не рисует динамический портрет жёстким `ScaleMode.ScaleAndCrop`. Он получает `portrait` из `DialogueSpeakerData`, находит layout-элемент `portrait` экрана `narrative-dialogue` и передаёт всё в единый `UILayoutRuntimeApplier.ApplyDynamicImage`.

`UILayoutRuntimeApplier` теперь является единым источником математики для динамических изображений и layout-фона:

- `ResolveImageScaleMode` переводит `Cover/Contain/Stretch` в Unity `ScaleMode`;
- `ResolveImageOffset` объединяет общий pixel-offset layout и индивидуальный offset говорящего в долях фактической рамки;
- `ResolveImageScale` объединяет общий layout-scale и индивидуальный scale, включая `Flip X`;
- `ApplyDynamicImage` применяет Sprite, scale mode, tint, opacity, zoom/pan и отражение через внутренний image-layer, сохраняя clipping рамки.

Если у говорящего нет своего Sprite, layout-изображение остаётся fallback. Placeholder по-прежнему показывается, когда нет ни динамического портрета, ни пригодного fallback.

### 4.3. База диалогов: визуальное кадрирование говорящего

Добавлен `DialogueDatabaseWindow.PortraitFraming.cs`. Во вкладке говорящих теперь есть секция **«КАДРИРОВАНИЕ ПОРТРЕТА»**:

- включатель «Индивидуальная настройка»;
- масштаб 0.05–4;
- числовое смещение X/Y в долях рамки;
- «Отразить по X»;
- кнопка сброса;
- preview с точными пропорциями реальной рамки экрана диалога;
- портрет можно **перетаскивать мышью прямо внутри preview**, меняя индивидуальное смещение.

Preview берёт общую рамку и настройки из UI Конструктора, поэтому автор видит сочетание общей композиции и индивидуальной поправки, а не отдельную несвязанную картинку.

`DialogueDatabaseWindow.PreviewAndSpeakers.cs` использует эту новую секцию вместо старого простого `ScaleToFit` preview. Новые говорящие создаются с нейтральными значениями кадрировки (`override=false`, scale=1, offset=0, flip=false).

### 4.4. UI Конструктор

`UILayoutEditorWindow.cs` приведён к той же математике изображения, что и runtime. Для экрана диалога preview динамического портрета учитывает:

- общий режим изображения;
- общий scale/offset/tint/opacity;
- индивидуальный scale/offset/flip выбранного говорящего.

Таким образом, UI Конструктор, База диалогов и игровой runtime больше не должны показывать три разные интерпретации одного портрета.

### 4.5. Затронутые файлы

Изменения текущего этапа ограничены портретным/UI-слоем:

- `Assets/_Project/DialogueDatabase/Runtime/DialogueDatabaseAsset.cs`;
- `Assets/_Project/DialogueDatabase/Editor/DialogueDatabaseWindow.PortraitFraming.cs` + `.meta`;
- `Assets/_Project/DialogueDatabase/Editor/DialogueDatabaseWindow.PreviewAndSpeakers.cs`;
- `Assets/_Project/DialogueDatabase/Editor/KingdomSurvival.DialogueDatabase.Editor.asmdef`;
- `Assets/_Project/UI/PrototypeUIController.Narrative.cs`;
- `Assets/_Project/UILayout/Runtime/UILayoutRuntimeApplier.cs`;
- `Assets/_Project/UILayout/Runtime/UILayoutScreenBinder.cs`;
- `Assets/_Project/UILayout/Editor/UILayoutEditorWindow.cs`;
- `Assets/_Project/Tests/EditMode/PortraitFramingTests.cs` + `.meta`.

Контент N01–N17, `NARRATIVE.md`, `LORE.md`, боевые формулы, StoryDirector и сохранения этой правкой не менялись.

## 5. Проверка текущей правки

### 5.1. Проверено в удалённой среде

- перед переносом сверены `main` и `work/dialogue-portrait-framing`;
- подтверждено, что `main` продвинулся до `112ff893`, а рабочая ветка оставалась на `c7032c1`;
- сравнение показало отсутствие прямых файловых пересечений: новый `main` менял `DialogueDatabaseWindow.Graph.cs`, `GraphPresentation.cs`, `InspectorPresentation.cs` и их тесты, текущая правка — другие файлы;
- итоговая реализация заново построена **поверх дерева `main`**, а не поверх старого дерева рабочей ветки;
- compare `112ff893 → d258408d` показывает `ahead_by: 1`, `behind_by: 0` и только 11 ожидаемых файлов портретного слоя;
- несвязанные изменения актуального `main` сохранены.

### 5.2. EditMode-тесты

Добавлен `PortraitFramingTests.cs` с 5 проверками:

1. нейтральные defaults `DialogueSpeakerData`;
2. объединение global pixel-pan и normalized speaker-pan;
3. объединение global/individual scale и `Flip X`;
4. масштабирование размеров layout-рамки между reference и actual resolution;
5. соответствие `Cover / Contain / Stretch` правильным Unity `ScaleMode`.

**Unity Editor, Unity compiler и Test Runner в подключённой среде недоступны, поэтому эти 5 тестов и полный regression Run All здесь фактически не запускались.** Текущий статус реализации — «код и diff подготовлены, нужна проверка в Unity».

## 6. Что проверить после Pull

Обязательная ручная проверка этого этапа:

1. дождаться Unity-компиляции и убедиться, что Console без C# errors;
2. запустить полный EditMode `Run All`, отдельно убедиться, что `PortraitFramingTests` зелёные;
3. открыть `Kingdom Survival → UI Конструктор → Диалог → Портрет`, проверить `Cover`, `Contain`, `Stretch`, общий Scale/Offset/Tint/Opacity;
4. открыть `Kingdom Survival → База диалогов → Говорящие`, выбрать нескольких персонажей разных пропорций, включить индивидуальную настройку и проверить scale, X/Y, `Flip X`, Reset и drag мышью внутри preview;
5. проверить тот же диалог в Play Mode: кадр должен совпадать с preview, а смена говорящего должна применять индивидуальную кадрировку нового персонажа;
6. отдельно проверить персонажа без индивидуального override — он должен использовать только общие настройки UI Конструктора;
7. проверить говорящего без Sprite — fallback/placeholder не должен ломать layout.

## 7. Что делать дальше

Если Unity-проверка зелёная, считать систему кадрирования портретов технически закрытой и вернуться к production-проходу первой главы. Следующий приоритет по-прежнему — конкретные сцены и причинная цепочка первой главы, а не расширение универсального UI-фреймворка.

Если обнаружится визуальное расхождение между UI Конструктором, preview Базы диалогов и Play Mode, исправлять нужно прежде всего общую математику `UILayoutRuntimeApplier`, а не добавлять третий набор локальных поправок.

Канон этой правкой не изменён.