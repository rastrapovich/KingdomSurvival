# Kingdom Survival — Development Status

Последнее обновление: 2026-09-03

> Технический журнал фактически реализованного состояния Unity-проекта.
> Актуальный канон основной игры: `KINGDOM_SURVIVAL_GAME_CONCEPT_CANON_RU_v1_14.md`.
> Старые подробные этапы сохранены в `ProjectDocs/Archive/DEVELOPMENT_STATUS_2026-09-02_PRE_BATTLEFIELD_DATABASE.md` и истории Git.

## 1. Текущий этап

Идёт развитие изолированного тактического полигона `Assets/_Project/Scenes/BattleSandbox.unity`.

Основная кампания (`Prototype_Main.unity`, `GameState`, стратегический `BattleSystem`, карта, гарнизоны, сохранения) этим этапом не изменена. Канон v1.14 также не изменён: конкретные правила ручной тактики всё ещё считаются sandbox-направлением до отдельной команды на фиксацию.

## 2. Текущее поле боя

- pointy-top гекс-сетка;
- 49 активных клеток;
- ряды сверху вниз: `6 / 7 / 8 / 7 / 8 / 7 / 6`;
- координатный контейнер `8 × 7`, семь внешних координат исключены из `IsInside`, pathfinding, AI и hit-test;
- odd-row half-hex offset сохранён;
- вертикальная проекция сетки: `GridVerticalScale = 0.75`;
- гексы и вертикальный шаг рядов сжаты по Y на 25%, миниатюры, HP и боевые иконки не деформируются;
- runtime и preview `Базы полей боя` используют одинаковую геометрию;
- активная арена визуально занимает около 70% ширины боевой сцены и центрирована внутри полноэкранного фонового изображения;
- прямоугольная рамка вокруг viewport арены убрана.

## 3. Боевая модель sandbox

- движение отделено от 1 ОД;
- остаток движения можно тратить несколькими перемещениями;
- обычный гекс стоит 1 движения, сложный — 2, непроходимый блокирует путь;
- ближняя атака допускается только если противник достижим в пределах оставшегося движения;
- центр гекса цели автоматически выбирает доступную сторону подхода, край сохраняет ручной выбор стороны;
- дальняя атака не зависит от стороны гекса;
- формула урона использует `Attack / Defense / Damage` и текущие sandbox-модификаторы;
- один ответный удар на бойца за раунд;
- ответ возникает только после ближней атаки, если защитник выжил;
- ответ не расходует ОД/движение и не создаёт новый ответ;
- выстрел ответ не вызывает;
- ответный удар восстанавливается в начале нового раунда;
- атака, ответ и движение имеют последовательные UI-анимации;
- ожидаемое число EditMode-тестов: `82`.

Ответный удар, конкретная формула урона и остальные детали sandbox пока не повышены до утверждённого канона основной игры.

## 4. База существ

`Kingdom Survival → База существ` остаётся единым источником типов бойцов и существ.

Работают:

- стабильный `typeId` вместо персонального имени на уровне типа;
- характеристики HP / атака / защита / урон / движение / инициатива / дальность;
- теги;
- вертикальный портрет с `ScaleAndCrop`, масштабом и смещением;
- отдельная миниатюра поля боя со своим масштабом и смещением;
- портреты используются в ростере, шкале инициативы и подробной карточке;
- миниатюры используются непосредственно на гекс-поле;
- HP-полосы и боевые оверлеи остаются отдельными UI Toolkit-слоями.

## 5. База полей боя

`Kingdom Survival → База полей боя` хранит:

- стабильный ID;
- название;
- фоновой Sprite;
- масштаб и смещение;
- теги;
- активное поле для `BattleSandbox`.

Фон не содержит нарисованных гексов. Сетка, маршруты, подсветка, препятствия и юниты накладываются отдельно.

Текущий preview показывает 49 гексов `6 / 7 / 8 / 7 / 8 / 7 / 6` с вертикальным коэффициентом `0.75`.

## 6. Текущая компоновка BattleSandbox

Последний UI-pass ориентирован на принцип: максимум пространства отдаётся сцене, постоянный UI не обрамляет экран тяжёлыми панелями.

Реализовано:

- фон поля занимает боевую сцену;
- гекс-арена центрирована и уменьшена примерно до 70% ширины;
- рамка вокруг арены убрана;
- шкала инициативы находится сверху по центру без общей полосы-подложки;
- заголовок `БОЕВОЙ ПОЛИГОН · ЧЁРНЫЙ ЛЕС` скрыт;
- слева сверху отображается только `Раунд N` без подписи `движение + одно боевое действие`;
- `НОВЫЙ СОСТАВ` остаётся компактной кнопкой справа сверху;
- нижний левый блок теперь содержит только заголовок `ХОД БОЯ` и прокручиваемый журнал;
- строки `ВАШ ХОД / ХОД ВРАГА`, название текущего бойца, его характеристики, состояние ответного удара и выбранная цель в этом постоянном блоке скрыты;
- постоянная обучающая инструкция скрыта;
- `ЗАЩИТНАЯ СТОЙКА` и `ЗАКОНЧИТЬ ХОД` находятся снизу справа;
- для трёх основных кнопок (`НОВЫЙ СОСТАВ`, `ЗАЩИТНАЯ СТОЙКА`, `ЗАКОНЧИТЬ ХОД`) работают явные normal / hover / pressed состояния;
- `ХОД БОЯ` помещён в вертикальный `ScrollView` с автоматическим скроллбаром;
- результат боя остаётся отдельным центральным overlay.

UI-pass выполнен через `BattleSandboxPresentationRunner.cs`, поэтому боевая runtime-модель и основная кампания не зависят от этой компоновки.

## 7. Файлы последнего UI hotfix

Изменены:

- `Assets/_Project/BattleSandbox/UI/BattleSandboxPresentationRunner.cs`;
- `ProjectDocs/DEVELOPMENT_STATUS.md`.

Боевые runtime-файлы, канон и данные существ/полей этим hotfix не изменены.

## 8. Что проверено статически

- ширина арены задаётся симметричными отступами `15% + 15%`, то есть остаётся около 70% ширины viewport;
- border-width у `battle-sandbox-board` принудительно обнулён;
- заголовок боя скрывается presentation-layer, `Раунд N` переносится на его место;
- presentation-layer удаляет суффикс после `·` из текущей строки раунда, поэтому старая подпись контроллера визуально не показывается;
- `currentUnit`, `currentStats`, `instruction` и `target` скрываются через `DisplayStyle.None`;
- нижняя левая панель уменьшена и оставляет только `ХОД БОЯ` с `ScrollView(Vertical)`;
- hover/pressed callbacks основных кнопок регистрируются один раз при создании нового боевого экрана;
- форма арены, вертикальный масштаб `0.75`, hit-test, pathfinding, AI, урон и ответный удар не менялись;
- ожидаемое число EditMode-тестов остаётся `82`.

Ограничение проверки: в подключённом окружении нет Unity Editor и C#-компилятора, поэтому фактическая Unity-компиляция, Play Mode и Test Runner должны быть проверены после pull.

## 9. Что проверить после pull

1. Дождаться Unity-компиляции без ошибок.
2. Открыть `BattleSandbox.unity` и начать бой.
3. Убедиться, что слева сверху написано только `Раунд N`.
4. Убедиться, что `движение + одно боевое действие` нигде рядом с раундом не видно.
5. Проверить, что в нижнем левом блоке остались только `ХОД БОЯ` и журнал.
6. Убедиться, что `ВАШ ХОД / ХОД ВРАГА`, название бойца, характеристики и цель не отображаются в этом блоке.
7. Накопить несколько сообщений в `ХОД БОЯ` и проверить колесо мыши / скроллбар.
8. Проверить hover и pressed-состояния `НОВЫЙ СОСТАВ`, `ЗАЩИТНАЯ СТОЙКА`, `ЗАКОНЧИТЬ ХОД`.
9. Проверить клики по гексам, ручной выбор грани, движение, выстрел, ответный удар и инициативу.
10. Запустить EditMode Test Runner; ожидается `82` успешных теста.
11. Открыть `Prototype_Main.unity` и проверить, что основная игра не изменилась.

## 10. Следующий шаг

После визуальной проверки решить, нужно ли дополнительно уменьшать сам блок `ХОД БОЯ` или менять его прозрачность. Следующие улучшения — только после отдельного решения.
